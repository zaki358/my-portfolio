import React from 'react'
import styled from 'styled-components'

export const Contact = () => {
  return (
    <SSection>Contact</SSection>
  )
}

const SSection = styled.section`
  padding-top: 60px;
`
