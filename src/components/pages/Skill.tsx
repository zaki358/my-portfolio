import React from 'react'
import styled from 'styled-components'

export const Skill = () => {
  return (
    <SSection>Skill</SSection>
  )
}

const SSection = styled.section`
  padding-top: 60px;
`
